# openpilot 핵심 개념

openpilot의 구조와 작동 원리를 이해하기 위한 핵심 개념 가이드입니다.

---

## 📚 목차

1. [시스템 아키텍처](#시스템-아키텍처)
2. [메시지 기반 아키텍처](#메시지-기반-아키텍처)
3. [주요 프로세스](#주요-프로세스)
4. [제어 시스템](#제어-시스템)
5. [비전 시스템](#비전-시스템)
6. [차량 인터페이스](#차량-인터페이스)

---

## 시스템 아키텍처

openpilot은 **모듈형 아키텍처**를 사용하여 각 기능을 독립적인 프로세스로 분리합니다.

### 전체 구조도

```
┌─────────────────────────────────────────────────────────┐
│                       Hardware                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐  │
│  │ Cameras  │  │   GPS    │  │   Panda (CAN)       │  │
│  └──────────┘  └──────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                         ▲
                         │
┌─────────────────────────────────────────────────────────┐
│                  Sensor Processes                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐  │
│  │ camerad  │  │locationd │  │   boardd/pandad     │  │
│  └──────────┘  └──────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                         │
                    cereal msgs
                         │
┌─────────────────────────────────────────────────────────┐
│                Perception & Planning                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐  │
│  │  modeld  │  │ plannerd │  │   calibrationd      │  │
│  └──────────┘  └──────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                         │
                    cereal msgs
                         │
┌─────────────────────────────────────────────────────────┐
│                      Control                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │                 controlsd                        │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   Vehicle (CAN)                         │
└─────────────────────────────────────────────────────────┘
```

### 핵심 설계 원칙

1. **분리 (Separation)**: 각 기능은 독립적인 프로세스
2. **통신 (Communication)**: cereal을 통한 메시지 전달
3. **안전 (Safety)**: panda에서 하드웨어 레벨 안전 보장
4. **실시간성 (Real-time)**: 100Hz 제어 루프

---

## 메시지 기반 아키텍처

openpilot은 **cereal**이라는 메시징 시스템을 사용합니다.

### cereal이란?

- Cap'n Proto 기반의 직렬화 라이브러리
- 프로세스 간 통신 (IPC) 제공
- Pub/Sub 패턴 사용

### 주요 메시지 타입

```python
# 1. 카메라 이미지
roadCameraState = {
  'image': bytes,
  'frameId': int,
  'timestampEof': int,
  ...
}

# 2. 모델 출력
modelV2 = {
  'laneLines': [...],
  'leadOne': {...},
  'position': {...},
  ...
}

# 3. 제어 명령
carControl = {
  'actuators': {
    'steer': float,
    'accel': float,
    ...
  },
  ...
}

# 4. 차량 상태
carState = {
  'vEgo': float,           # 차량 속도
  'steeringAngleDeg': float,
  'gas': float,
  'brake': float,
  ...
}
```

### 메시지 흐름 예시

```
camerad → roadCameraState → modeld
                ↓
            modelV2 → plannerd
                ↓
          pathPlan → controlsd
                ↓
          carControl → panda → Vehicle
```

---

## 주요 프로세스

### 1. **camerad** - 카메라 관리

```python
# 역할
- 카메라에서 이미지 캡처
- 이미지 전처리 및 인코딩
- roadCameraState 메시지 발행

# 주요 파일
system/camerad/
```

### 2. **modeld** - 비전 모델

```python
# 역할
- 딥러닝 모델(supercombo) 실행
- 차선, 경로, 다른 차량 인식
- modelV2 메시지 발행

# 주요 파일
selfdrive/modeld/

# 모델 입력
- 카메라 이미지 (1164x874)
- 이전 상태

# 모델 출력
- 차선 위치
- 주행 경로
- 선행 차량 위치 및 속도
- 운전 가능 공간
```

### 3. **plannerd** - 경로 계획

```python
# 역할
- modeld의 출력을 기반으로 주행 경로 생성
- 속도 프로파일 계산
- lateralPlan, longitudinalPlan 메시지 발행

# 주요 파일
selfdrive/controls/plannerd.py

# 기능
- 차선 변경 계획
- ACC (적응형 크루즈 컨트롤) 로직
- 커브 속도 제한
```

### 4. **controlsd** - 제어

```python
# 역할
- 실제 차량 제어 명령 생성
- 조향, 가감속 제어
- carControl 메시지 발행

# 주요 파일
selfdrive/controls/controlsd.py

# 제어 루프
- 주파수: 100Hz
- PID/MPC 컨트롤러 사용
- 안전 검사 수행
```

### 5. **calibrationd** - 캘리브레이션

```python
# 역할
- 카메라 장착 각도 자동 보정
- IMU 캘리브레이션
- liveCalibration 메시지 발행

# 주요 파일
selfdrive/locationd/calibrationd.py

# 방법
- 주행 데이터 분석
- Kalman 필터 사용
- 지속적 업데이트
```

### 6. **locationd** - 위치 추정

```python
# 역할
- GPS + IMU 융합
- 정확한 위치 및 속도 추정
- liveLocationKalman 메시지 발행

# 주요 파일
selfdrive/locationd/

# 센서 융합
- GPS (낮은 주파수, 높은 정확도)
- IMU (높은 주파수, 낮은 정확도)
- 차량 오도메트리
```

---

## 제어 시스템

### 횡방향 제어 (Lateral Control)

차량의 좌우 움직임(조향) 제어

#### PID 컨트롤러 (기본)

```python
# PID 제어 로직 (의사 코드)
error = desired_path - current_position
P = Kp * error
I = Ki * integral(error)
D = Kd * derivative(error)
steering = P + I + D
```

#### MPC (Model Predictive Control)

- 더 정교한 제어
- 미래 경로 예측
- 제약 조건 고려

### 종방향 제어 (Longitudinal Control)

차량의 전후 움직임(가감속) 제어

#### ACC (Adaptive Cruise Control)

```python
# ACC 로직 (의사 코드)
if lead_car_detected:
    target_speed = calculate_safe_speed(
        lead_distance,
        lead_speed,
        desired_following_distance
    )
else:
    target_speed = set_speed

acceleration = pid_controller(target_speed - current_speed)
```

---

## 비전 시스템

### supercombo 모델

openpilot의 메인 딥러닝 모델

#### 입력

- **이미지**: 1164x874 RGB (road camera)
- **과거 상태**: 이전 프레임 정보
- **desire**: 차선 변경 의도

#### 출력

1. **차선 (Lane Lines)**
   - 좌/우 차선 위치
   - 차선 확률

2. **경로 (Path)**
   - 향후 주행 경로
   - 3초간의 예측

3. **선행 차량 (Lead)**
   - 위치 (x, y)
   - 속도
   - 가속도

4. **메타 데이터**
   - 도로 가장자리
   - 운전 가능 공간

### 모델 아키텍처

```
Input Image (1164x874x3)
         ↓
  Encoder (CNN)
         ↓
   Temporal RNN
         ↓
  Multiple Heads
    ├── Path
    ├── Lanes
    ├── Lead
    └── Meta
```

---

## 차량 인터페이스

### panda

하드웨어 안전 장치

#### 역할

1. **CAN 통신**: 차량과 통신
2. **안전 모델**: 위험한 명령 차단
3. **GPS 수신**: 위치 정보 제공

#### 안전 모델 예시

```c
// panda 안전 모델 (의사 코드)
bool safe_check(can_msg) {
  // 1. 조향 범위 체크
  if (abs(steer) > MAX_STEER) return false;
  
  // 2. 조향 변화율 체크
  if (abs(steer - prev_steer) > MAX_RATE) return false;
  
  // 3. 운전자 개입 확인
  if (driver_torque_detected) disengage();
  
  return true;
}
```

### opendbc

차량 CAN 데이터베이스

#### DBC 파일 구조

```dbc
BO_ 37 STEER_ANGLE_SENSOR: 8 XXX
 SG_ STEER_ANGLE : 3|12@0+ (0.1,-600) [0|0] "deg" XXX
 SG_ STEER_FRACTION : 39|4@0+ (0.1,0) [0|0] "" XXX
 SG_ STEER_RATE : 35|12@0- (1,0) [-2000|2000] "deg/s" XXX

BO_ 228 STEERING_CONTROL: 5 XXX
 SG_ STEER_TORQUE : 7|16@0- (1,0) [-4096|4096] "" XXX
 SG_ SET_ME_1 : 23|8@0+ (1,0) [0|0] "" XXX
 SG_ CHECKSUM : 39|8@0+ (1,0) [0|0] "" XXX
```

---

## 데이터 흐름 종합

### 일반적인 주행 사이클 (100Hz)

```
1. camerad: 이미지 캡처 → roadCameraState
2. modeld: 이미지 분석 → modelV2
3. plannerd: 경로 계획 → lateralPlan, longitudinalPlan
4. controlsd: 제어 명령 생성 → carControl
5. panda: 안전 검사 후 CAN 전송
6. 차량: 명령 실행
7. boardd: 차량 상태 읽기 → carState
8. 반복...
```

### 지연 시간 (Latency)

- 카메라 → 모델: ~50ms
- 모델 → 제어: ~20ms
- 제어 → 차량: ~30ms
- **총 지연: ~100ms**

---

## 디버깅 팁

### 1. 메시지 모니터링

```bash
# 실시간 메시지 확인
cereal log cat roadCameraState

# 여러 메시지 동시 확인
cereal log cat modelV2 carControl carState
```

### 2. 프로세스 상태 확인

```bash
# 실행 중인 프로세스
tmux attach

# 로그 확인
tail -f /data/log/swaglog
```

### 3. Replay 사용

```bash
# 주행 데이터 재생
./tools/replay/replay /path/to/log

# UI와 함께
./tools/replay/replay --ui /path/to/log
```

---

## 학습 순서 추천

1. ✅ **메시지 흐름 이해**: cereal 메시지 파악
2. ✅ **주요 프로세스**: controlsd, plannerd, modeld
3. ✅ **제어 로직**: PID/MPC 컨트롤러
4. ✅ **비전 모델**: supercombo 입출력
5. ✅ **안전 모델**: panda 안전 검사
6. ✅ **차량 인터페이스**: opendbc, CAN 통신

---

## 다음 단계

- **[코드 분석](./code-analysis.md)**: 실제 코드 읽기
- **[파라미터 튜닝](../how-to/parameter-tuning.md)**: 제어 파라미터 조정
- **[새 차량 포팅](../car-porting/)**: 차량 추가 방법

---

## 참고 자료

- [공식 문서](https://docs.comma.ai)
- [cereal 문서](https://github.com/commaai/cereal)
- [opendbc](https://github.com/commaai/opendbc)
- [panda](https://github.com/commaai/panda)

---

<div align="center">

**openpilot의 작동 원리를 이해하셨나요? 🧠**

더 깊이 학습하려면 실제 코드를 읽어보세요!

</div>
